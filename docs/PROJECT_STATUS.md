# PROJECT STATUS

最終更新: 2026-09-23

## 正本

- ゲーム仕様: `docs/GAME_SPEC.md`
- UI仕様: `docs/UI_SPEC.md`
- 優先差分仕様: `docs/ADDITIONAL_SPEC_v0.5.md`
- 最新優先差分仕様: `docs/INTEGRATED_ADDITIONAL_SPEC.md`
- 統合追加修正仕様書、v0.5、既存仕様の順で、競合箇所のみ新しい文書を優先する
- PITCH LAB / soccer-Simulatorとは完全に別リポジトリ・別コードベース

## 現在位置

**Stage 17 — 局所仕様復元・修正／実装完了**

Stage 17の実装:

- 既存24特殊能力を、本人・攻撃タイプ・好機・残り時間・点差ごとの正式条件へ修正
- Season 2以降のドラフトを第1巡同時、第2〜4巡前年順位順へ修正
- 順位タイブレークを当該クラブ間成績、seeded randomまで含めた順序へ修正
- 放出された通常選手を将来のオークション候補へ復帰可能に変更（初期配布選手は消滅）
- 29〜34歳の能力別衰退と35歳引退を正式仕様へ修正
- 新規自動テスト・回帰テストはユーザー指示により追加・実行していない

Stage 16の実装:

- 18〜23歳を対象とした能力覚醒（基本3%、育成・出場補正、再覚醒は25%）
- 能力別hiddenGrowthを重みとする2能力の覚醒ボーナス（+3〜+5）
- 特殊能力なし選手の後天的特殊能力習得（最大1つ、既存24種類のみ）
- 成長結果に同ランク成長の「↑」、覚醒、特殊能力習得を表示
- 人間・CPU共通のオフシーズン処理とseeded randomを使用
- 自動テスト・回帰テストはユーザー指示により実行していない
- GitHub Pages公開版 `v0.11.0・Stage 16` の表示を確認

直近のStage 15実装:

- 通常進行を「この節を進める」から「シーズンをシミュレート」へ変更
- 既存の1節処理を順番に実行し、開幕時は10節・リーグ全30試合を一括処理
- 途中節の旧セーブからは残りの節だけを処理可能
- 人間クラブの10試合について、節、対戦相手、ホーム／アウェー、スコア、勝敗、選手成績、イベント、調子を保存
- シーズン終了後に最終順位、勝点、勝、分、敗、得点、失点、得失点差を表示
- 10試合を○／△／●、ホーム／アウェー、対戦相手、スコア付きで一覧表示
- 各試合の得点者、アシスト、MVP、評価、得点、アシスト、シュート、攻撃貢献、守備成功、セーブ、調子、イベントログを表示
- 所属選手の出場、得点、アシスト、平均評価、シュート、攻撃貢献、守備成功、セーブを表示
- 一括進行後も既存の順位表、表彰、オフシーズン進行を維持
- 旧セーブに `seasonResults` がない場合は空配列として補完

Stage 14の実装:

Stage 14の実装:

- 編成画面にGK / FIXO / ALA / ALA / PIVOの5枠を追加
- 所属選手を選択し、配置枠を押して `club.lineup` を変更できる操作を追加
- 既存スタメンを別枠へ移す場合は枠内の選手を入れ替え、重複配置を防止
- 人間とCPUで共通の `SET_LINEUP` 検証・状態更新を使用
- 5枠未設定、重複、所属外選手、GKのフィールド枠配置をエラーとして拒否
- 本職外配置を警告し、本職100%・隣接95%・その他フィールド85%・フィールド選手のGK能力50%という既存適性を適用
- スタメンと控えを分離し、全選手の総合・能力・特殊能力をカード上で比較可能に変更
- 既存の選手詳細表示を維持
- 有効なスタメン5人が揃っていない場合は試合開始を禁止
- `club.lineup` の枠順を試合計算へ反映し、控え選手が試合成績へ混入していた旧処理を修正

Stage 13の実装:

- 統合追加修正仕様書を `docs/INTEGRATED_ADDITIONAL_SPEC.md` として保存
- 同一Club構造へ `controllerType: HUMAN / CPU` を追加
- HumanClub / CPUClubの別データ型は作らず、全クラブで同じデータ形式を維持
- CPUのドラフト指名・競売入札を、状態変更ではなくAction生成として分離
- 人間とCPUの `DRAFT_PICK` を同じ指名集計・競合抽選処理へ提出
- 人間とCPUの `AUCTION_BID` を同じ入札検証・落札・資金減算・移籍処理へ提出
- 編成、戦術、育成選択、契約更新、放出の共通Action型と検証処理を追加
- CPUのスタメン・戦術・契約判断を意思決定と共通状態更新へ分離
- 試合シミュレーションは `controllerType` を参照せず、クラブ・スタメン・戦術・seedだけを使用
- 新規処理から「クラブID 1だけが人間」という固定参照を除去
- 旧セーブに `controllerType` がない場合、読込時に既存 `humanClubId` から補完
- Cloudflare、REMOTE通信、ログイン、ルーム、WebSocket等は実装していない

Stage 12の実装:

- Season 1の全4巡を人間クラブ＋CPU5クラブの完全同時指名へ変更
- 同じ候補への指名だけを抽選し、外れクラブは残った候補から再指名
- 人間クラブが辞退した場合もCPU5クラブは各巡の指名を最後まで完了
- Season 2以降も毎年、新しい固有IDのドラフト24候補と競売18候補を生成
- オフシーズン終了後に次年度ドラフト、競売、編成、戦術、シーズンの順で進行
- 全6クラブへ年間基本資金+100pt、1位+10pt、2位+5ptを適用
- 全クラブの持越し資金を150pt以内に制限し、同じシーズンの二重加算を防止
- 契約満了者の更新・放出が終わるまで次年度市場へ進めないよう画面フローを修正
- CPUが将来のポジション不足と34歳以上の選手を考慮し、市場前に補強枠を確保
- 10シーズンを通して全クラブが補強・試合・資金・育成・契約・世代交代を完走する統合検証を追加
- シーズン記録を成長・引退処理前に確定し、最終シーズンまで履歴を保持

Stage 11の実装:

- CPU5クラブが毎シーズン、全ロスターからGK 1 / FIXO 1 / ALA 2 / PIVO 1のスタメンを再選出
- 適性補正後のポジション別Overallを基準に、新加入選手も含めて起用判断
- CPUのスタメン能力と特殊能力からポゼッション / ドリブル / カウンター / バランスを自動設定
- CPUが公開能力、年齢、起用実績から育成対象2人と重点能力を選択
- CPU育成選考では `hiddenGrowth` を直接参照せず、実際の成長処理だけに能力別 `hiddenGrowth` を使用
- 人間クラブを含む全6クラブへ加齢、成長、衰退、35歳引退、契約年数減少を適用
- CPUが年齢、Overall、ポジション人数、出場実績、更新費、資金から契約更新・放出を判断
- CPU放出時も最低5人とGK最低1人を保護
- ドラフト・競売後および次シーズン開始時にCPUのスタメンと戦術を更新
- 成長結果画面にCPU5クラブも年度処理を完了したことを表示

Stage 10の実装:

- 公開情報だけを扱う共通選手カードを追加
- 編成、ドラフト、競売、育成、契約、試合結果、シーズン表彰、個人成績で能力ランクを常時表示
- フィールド選手はシュート・走力・守備・ドリブル・パス、GKはGK能力も表示
- 総合ランク、年齢、国籍、ポジション、契約残年数を同じカードで比較可能
- 主要カードへ `★ 特能名` を表示し、特能なしも明示
- 選手詳細パネルに特殊能力の説明と今季成績を表示
- ドラフト・競売へ「所属選手を見る」ボタンを常設
- 所属選手パネルにGK / FIXO / ALA / PIVOの人数と全選手情報を表示
- 個人成績画面を追加
- 試合結果の出場選手とシーズンMVP・Best 5にも能力カードを表示
- 主要な見出し、ボタン、戦術、育成能力、セーブ操作を日本語化
- PCは自動段組み、スマホは1列になるレスポンシブカードと全画面パネルを追加
- UIへ内部能力値と `hiddenGrowth` を渡さず、ランクだけを描画
- Stage 10修正：市場候補の全能力を同一ティア範囲にする旧生成方式を廃止
- Stage 10修正：GK / FIXO / ALA / PIVOごとに3種類の能力タイプと個体差を追加
- Stage 10修正：能力ランクを分散させながら、重み付きOverallは抽選された候補ティア内に維持

Stage 9の実装:

- `hiddenGrowth` を単一値から能力別オブジェクトへ変更
- 初期選手は能力別0.55〜0.75、通常選手は0.70〜1.30で生成
- 成長計算が対象能力ごとの `hiddenGrowth` を使うよう修正
- FIXO / ALA / PIVOを正本のポジション別重みでOverall計算
- GKはGK能力70%を中心とする重み付きOverallへ変更
- GK / FIXO / ALA / PIVOの特殊能力を各6種、計24種へ復元
- 市場候補の特殊能力あり40%・なし60%を維持
- 現在能力、年齢、能力別成長傾向、特殊能力からスカウトコメントを生成
- 低確率の総合成長タイプコメントを追加
- 苗字152種・名前114種へ拡張し、seeded randomで独立抽選
- 旧セーブの単一値 `hiddenGrowth` も成長処理で読み取れる互換処理

既存実装:

- Stage 1〜8の基本UI、試合、リーグ、ドラフト、競売、育成、契約、特殊能力、戦術、10シーズン、記録、セーブ、バッチ検証
- GitHubリポジトリ `Shunichitube/football-league`
- GitHub Pages公開 `https://shunichitube.github.io/football-league/`

## 動作確認

2026-09-23に以下を確認済み。

- `npm test`：48件成功
- 既存の同seed再現、80フェーズ、全30試合、10シーズン、セーブ、100リーグ・3,000試合バッチが継続成功
- 特殊能力生成率が36〜44%の範囲に収まり、全24種が生成可能
- 能力別 `hiddenGrowth` の範囲と能力別成長差を確認
- ポジション別Overallの重みを確認
- スカウトコメントが成長傾向で変わり、レアコメントも生成可能
- 苗字152種・名前114種、同seedで同じ名前列を再現
- 全能力ランク、GKランク、特殊能力、契約残年数を含む公開カードを確認
- 選手詳細で特殊能力説明と今季成績が表示されることを確認
- 所属選手パネルのポジション人数（GK 1 / FIXO 1 / ALA 2 / PIVO 1）を確認
- 公開カードと詳細HTMLに内部能力値および `hiddenGrowth` が含まれないことを確認
- `node --check js/app.js` / `node --check js/ui.js`：構文エラーなし
- GitHub Pages公開版で、新規開始 → ドラフト能力表示 → 所属選手パネル → 選手詳細 → 閉じる、を実操作確認
- 公開版の所属人数がGK 1 / FIXO 1 / ALA 2 / PIVO 1で、操作後の画面エラーがないことを確認
- 4,200人の市場候補で、表示Overallと抽選ティアが全員一致することを確認
- 市場候補の85%以上で能力ランクが2種類以上に分散し、総合A候補は全員が複数ランク構成になることを確認
- GK能力、FIXO守備、ALA走力・ドリブル、PIVOシュートが各ポジションの平均的な強みになることを確認
- 強い新加入選手がCPUのスタメンへ再選出されることを確認
- CPU戦術がスタメンのパス・ドリブル・走力に応じて変更されることを確認
- CPU育成選考が `hiddenGrowth` の値を変えても影響を受けないことを確認
- 全6クラブで年齢・成長・衰退・契約年数が進行することを確認
- CPUの契約更新・放出判断と、最低5人・GK最低1人の維持を確認
- CPU5クラブの毎シーズン編成・戦術設定を確認
- 6クラブ完全同時指名、競合抽選、外れ再指名を確認
- 完全同時指名4巡で全6クラブが各巡1人を獲得できることを確認
- 全クラブの年間基本資金、順位賞金、150pt上限、二重加算防止を確認
- Season 2以降のドラフト・競売候補が毎年固有IDで生成されることを確認
- 10シーズンすべてで補強・30試合・資金更新・育成・契約・世代交代を完走
- 10シーズン終了時も全CPUクラブが5人以上とGKを維持することを確認
- ClubとControllerが同じClub構造の `controllerType` で分離されることを確認
- HUMANを2クラブへ増やしてもController抽出が固定IDに依存しないことを確認
- 人間とCPUのドラフトActionが同じ共通ルール処理を使用することを確認
- 人間とCPUの競売Actionが同じ共通ルールで資金・移籍を処理することを確認
- 編成・戦術・契約ActionがController種別に依存しないことを確認
- `controllerType` をHUMAN / CPU / REMOTE相当に変更しても同seedの試合結果が変わらないことを確認
- `controllerType` を持たない旧セーブがHUMAN 1 / CPU 5へ補完されることを確認
- `node --check js/cpu.js` / `node --check js/app.js`：構文エラーなし
- GitHub Pages公開版で `v0.6.0・Stage 11`、新規ゲーム開始、ドラフト24候補表示、画面エラーなしを実操作確認
- GitHub Pages公開版で `v0.7.1・Stage 12`、完全同時指名表示、24候補表示、1巡目獲得後の第2巡移行、画面エラーなしを実操作確認
- GitHub Pages公開版で `v0.8.0・Stage 13`、新規リーグ生成、完全同時指名24候補表示、1巡目獲得後の第2巡移行、アプリ由来の画面エラーなしを実操作確認
- GK / FIXO / ALA / ALA / PIVOの5枠、選手選択、枠入替え、重複防止、適性外警告を確認
- スタメンと控えの分離、全能力ランク、特殊能力、選手詳細の維持を確認
- 変更したスタメンだけが実際の試合結果と個人成績へ含まれることを確認
- GitHub Pages公開版 `v0.9.1・Stage 14` で、5枠表示、控え選択、枠配置、適性外警告、GK誤配置エラー、変更選手の試合出場、控え選手の結果除外、アプリ由来の画面エラーなしを実操作確認
- 一括進行で10節・全30試合を処理し、人間クラブ10試合を保存することを確認
- 途中節からは残り試合だけを処理し、重複保存しないことを確認
- 10試合一覧、各試合詳細、試合ごとの調子、所属選手のシーズン個人成績を確認
- `git diff --check`：空白エラーなし

## 次に行うこと

次のStageは未定。明示的な追加仕様を受けてから着手する。
